{
  "hooks": {
    "SessionStart": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "cd \"${CLAUDE_PLUGIN_ROOT}\" && { [ -d node_modules ] && diff -q package.json \"${CLAUDE_PLUGIN_DATA}/package.json.stamp\" >/dev/null 2>&1; } || { npm install --production && cp package.json \"${CLAUDE_PLUGIN_DATA}/package.json.stamp\"; }"
          }
        ]
      }
    ]
  }
}
